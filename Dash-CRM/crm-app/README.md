# Ateliux CRM (Frontend)

## Logins de demo (seed)

Senha: `Ateliux123!`

- ADMIN: admin@ateliux.local
- MANAGER: manager@ateliux.local
- SALES: sales@ateliux.local
- SUPPORT: support@ateliux.local
- VIEWER: viewer@ateliux.local

## Instalação

Sempre rode as instalações na raiz do repositório:

```
cd crm-app
npm install
```

Não rode `npm install` dentro de `apps/web` ou `packages/*`.
